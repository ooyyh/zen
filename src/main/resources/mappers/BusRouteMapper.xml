<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hbnu.zen.mapper.BusRouteMapper">
    <resultMap id="RouteMap" type="com.hbnu.zen.mybatis.entity.BusRoute">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="origin" property="origin" />
        <result column="destination" property="destination" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into bus_route (name, origin, destination, status)
        values (#{name}, #{origin}, #{destination}, #{status})
    </insert>

    <update id="update">
        update bus_route
        set name = #{name},
            origin = #{origin},
            destination = #{destination},
            status = #{status}
        where id = #{id}
    </update>

    <select id="selectById" resultMap="RouteMap">
        select * from bus_route where id = #{id}
    </select>

    <select id="selectAll" resultMap="RouteMap">
        select * from bus_route order by created_at desc
    </select>
</mapper>
