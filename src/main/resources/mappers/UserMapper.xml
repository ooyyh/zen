<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hbnu.zen.mapper.UserMapper">
    <resultMap id="UserMap" type="com.hbnu.zen.mybatis.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password_hash" property="passwordHash" />
        <result column="role" property="role" />
        <result column="real_name" property="realName" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="selectByUsername" resultMap="UserMap">
        select * from `user` where username = #{username}
    </select>

    <select id="selectById" resultMap="UserMap">
        select * from `user` where id = #{id}
    </select>

    <select id="selectAll" resultMap="UserMap">
        select * from `user` order by id desc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into `user` (username, password_hash, role, real_name, status)
        values (#{username}, #{passwordHash}, #{role}, #{realName}, #{status})
    </insert>
</mapper>
